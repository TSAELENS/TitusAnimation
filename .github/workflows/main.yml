name: Deploy Symfony to OVH /www (final, stable, sans --input)

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v3

      - name: 🔧 Install git-ftp
        run: sudo apt-get update && sudo apt-get install -y git-ftp

      - name: 💾 Download remote .git-ftp.log
        env:
          FTP_SERVER: ${{ secrets.FTP_SERVER }}
          FTP_USERNAME: ${{ secrets.FTP_USERNAME }}
          FTP_PASSWORD: ${{ secrets.FTP_PASSWORD }}
        run: |
          echo "📡 Téléchargement de .git-ftp.log depuis le FTP..."
          curl --fail -s -u "$FTP_USERNAME:$FTP_PASSWORD" "ftp://$FTP_SERVER/www/.git-ftp.log" -o .git-ftp.log || echo "⚠️ Aucun fichier .git-ftp.log trouvé"
          echo "📄 SHA1 récupéré :"
          cat .git-ftp.log || echo "❌ Fichier vide"

      - name: 🚀 Déploiement vers OVH /www
        env:
          FTP_SERVER: ${{ secrets.FTP_SERVER }}
          FTP_USERNAME: ${{ secrets.FTP_USERNAME }}
          FTP_PASSWORD: ${{ secrets.FTP_PASSWORD }}
        run: |
          git config git-ftp.url "ftp://$FTP_SERVER/www"
          git config git-ftp.user "$FTP_USERNAME"
          git config git-ftp.password "$FTP_PASSWORD"
          git config git-ftp.syncroot .
          git ftp push
