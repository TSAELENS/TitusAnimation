{% extends 'base.html.twig' %}

{% block title %}Galerie - Photos{% endblock %}

{% block body %}
<section class="container mx-auto px-4 py-8">
  <h1 class="text-4xl font-bold mb-6 text-center">Galerie - Photos</h1>
  <p class="text-center text-gray-600 mb-8">Découvrez notre collection de photos mettant en avant l'art du ballon.</p>
  <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 gap-4">
    {% for image in images %}
    <div class="overflow-hidden rounded-lg shadow-lg">
      <img src="{{ asset('uploads/gallery/' ~ image.image) }}" alt="{{ image.titre }}" class="w-full h-64 object-cover hover:scale-105 transition duration-300">
    </div>
  {% endfor %}
  </div>
</section>
{% endblock %}
